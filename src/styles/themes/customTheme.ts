import { createTheme } from '@nextui-org/react'

export const theme = {
	dark: createTheme({
		type: 'dark',
		theme: {
			colors: {},
			space: {},
			fonts: {},
		},
	}),
	light: createTheme({
		type: 'light',
		theme: {
			colors: {},
			space: {},
			fonts: {},
		},
	}),
}
